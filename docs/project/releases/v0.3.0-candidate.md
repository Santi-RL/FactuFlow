# FactuFlow v0.3.0 — dossier de candidato

Estado: **CANDIDATO CONGELADO PARA REVISIÓN; NO PUBLICADO NI DESPLEGADO**.

Última actualización: 09/08/2026.

Este documento prepara la primera matriz contable de PF-16G para el corte
provisional `v0.3.0`. El contenido y la evidencia local disponible quedan
congelados para revisión, pero no existe todavía un commit candidato
identificado. El dossier no crea una release ni autoriza tag, publicación o
despliegue. La producción continúa en `v0.2.2` hasta que esas decisiones se
adopten y ejecuten por separado.

## Decisión de corte

La base productiva es el tag `v0.2.2`, publicado y desplegado el 23/07/2026.
El corte propuesto incorpora PF-02, PF-03A y PF-19A/B/C: cambia defensas sobre
numeración, validación de payload superior, elegibilidad RECE y tratamiento de
un rechazo global ARCA. Es una unidad de riesgo Nivel 2.

El rango de release todavía no puede congelarse. Al redactar este dossier,
PF-19C y sus artefactos de soporte siguen sin commit en el árbol de trabajo;
por ello no hay SHA candidato, conteo definitivo de commits/archivos ni
resultado de CI que puedan atribuirse honestamente a `v0.3.0`. El rango final
debe iniciar en `v0.2.2` y terminar en el commit candidato que se integre y
verifique al cerrar el corte; esta frase describe una condición de cierre, no
asigna un identificador ficticio.

El checkpoint técnico local ya sincroniza la versión `0.3.0` y completa PF-19C
con dobles/SQLite. No convierte el árbol en una release: PostgreSQL real, CI y
ensayo de migración/restauración siguen pendientes. El `autoreview` final
autorizado ya cerró limpio de forma local.

## Alcance incluido

1. **PF-02:** numeración individual y masiva compatible con historia externa,
   segundo preflight, reintento manual seguro y recuperación stale sin
   asignación inferida ni reemisión automática.
2. **PF-03A:** rechazo temprano de claves superiores desconocidas en la emisión
   individual y revalidación fail-closed de snapshots batch manipulados.
3. **PF-19A:** contención adicional configurable por ambiente, emisor, punto y
   tipo, más inventario legacy estrictamente de solo lectura.
4. **PF-19B:** elegibilidad RECE durable, atestación administrativa acotada,
   snapshots/revalidaciones antes de CAE y exposición sanitaria de estado.
5. **PF-19C:** clasificación estricta del único error global `10005` aceptado,
   cierre terminal acotado, bloqueo de respuestas ambiguas y resolución legacy
   administrativa auditable.
6. **Migración `b9c0d1e2f3a4`:** elegibilidad RECE fail-closed, con ledger,
   cabeza por ambiente, snapshots y guardas fiscales relacionadas.
7. **Migración `c0d1e2f3a4b`:** evidencia ARCA estructurada y journal
   append-only para resoluciones legacy PF-19C.
8. Dossier y matriz contable/QA PF-16G del rango final, con resultados
   sanitizados y separación entre evidencia local, CI y operación VPS.

## Alcance excluido

- PF-03B: DTO de ítems, descuentos y valores no finitos;
- PF-16F y PF-16H;
- reconstrucción histórica general PF-05;
- permisos multiemisor PF-06/PF-07/PF-08/PF-09;
- automatización, retención o custodia externa de backups;
- cualquier solicitud de CAE o provocación de `10005` real;
- saneamiento manual de registros fiscales, edición de CAE o recreación de
  comprobantes;
- firma externa verificable de manifests VPS: es P2 posterior, no bloqueante y
  no reatesta ni reconstruye el journal PF-19C;
- tag, GitHub Release, despliegue, cambios permanentes en VPS o datos
  productivos durante la preparación local.

## Invariantes contables y fiscales del corte

- La fecha de emisión sigue siendo explícita, visible en `DD/MM/AAAA` y nunca
  se infiere de la fecha actual.
- La numeración externa solo diagnostica; una reserva y una emisión requieren
  las guardas, preflights e idempotencia correspondientes.
- Una clave desconocida no puede derivar en defaults silenciosos, operación
  idempotente, reserva, intento ni solicitud de CAE.
- RECE se acredita solo con la evidencia permitida; una sincronización técnica,
  un texto editable o una regla PF-19A no habilitan un punto.
- La contención PF-19A solo deniega la tupla exacta y nunca promueve RECE.
- Solo un `10005` entero no booleano, único y global, bajo una cabecera `R`
  completamente correlacionada y sin detalle ni CAE, puede ser terminal.
- Todo dato ambiguo, contradictorio o incompleto permanece en reconciliación;
  no habilita reintento fiscal ni emisión posterior.
- Un replay terminal devuelve el resultado durable sin WSAA, consultas de
  numeración, `FECompConsultar` ni `FECAESolicitar` nuevos.
- La resolución legacy solo confirma ausencia de autorización mediante consultas
  seguras; nunca atribuye retrospectivamente un rechazo a `10005`.

## Inventario técnico que deberá confirmar el rango final

| Área | Cambio esperado en el candidato | Riesgo de salida |
|---|---|---|
| Numeración | PF-02 individual, batch, reintento y stale | Alto: duplicación, salto o reemisión |
| API de emisión | PF-03A y manejo estructurado PF-19C | Alto: payload o cierre fiscal incorrecto |
| ARCA/WSFE | Elegibilidad RECE y rechazo global estricto | Alto: solicitud de CAE indebida o clasificación errónea |
| Lotes/worker | Guardas, ownership, replay y detención del lote | Alto: concurrencia e incertidumbre post-ARCA |
| Esquema | `b9c0d1e2f3a4` y `c0d1e2f3a4b` | Alto: integridad, migración y downgrade |
| UI | Estado RECE y selectores que consumen la autoridad del servidor | Medio/alto: habilitación visual engañosa |
| Operación | Backup, restore, rollback, paquete VPS y smoke | Alto: recuperación incompleta |
| Documentación | PF-16G, manual QA, diseño y dossier | Operativo: evidencia insuficiente |

Antes de congelar el rango, se debe contrastar esta tabla contra el diff real
desde `v0.2.2`, incluyendo dependencias, lockfiles, Docker/compose,
configuración y migraciones. Este documento no afirma todavía que todos esos
componentes hayan cambiado ni que sus validaciones hayan pasado.

## Migraciones y estrategia de recuperación

### `b9c0d1e2f3a4` — elegibilidad RECE fail-closed

Parte de `a8b9c0d1e2f3`. Antes del DDL verifica ownership y relaciones legacy
de puntos, operaciones, intentos, lotes y grupos; ante conflicto aborta sin
inferir ni corregir datos. En SQLite exige además backup físico distinto,
verificado por `quick_check`, `foreign_key_check` y equivalencia semántica con
la base activa.

El upgrade agrega la revisión fiscal del punto, ledger y cabeza RECE por punto
y ambiente, asociaciones compuestas por emisor, snapshots en los consumidores
fiscales y guardas de operación. Backfillea los puntos legacy en estado no
verificado y valida al terminar el DDL y los invariantes resultantes.

El downgrade es sensible: exige el mismo control de backup SQLite cuando
corresponde y verificaciones explícitas antes y después de retirar las
estructuras. No es un remedio automático ante un fallo de producción. Si hay
incertidumbre sobre datos, snapshots o estado fiscal, se detiene el servicio y
se decide entre rollback de aplicación compatible, downgrade ensayado o
restauración aislada del backup verificado.

### `c0d1e2f3a4b` — journal legacy PF-19C

Parte de `b9c0d1e2f3a4`. Agrega `errores_arca_json` a los intentos, una clave
única compuesta de intento/emisor y el journal
`resoluciones_legacy_pf19_journal`. El journal relaciona intento y emisor,
exige hashes y resultado/acción cerrados, permite una única resolución por
intento y bloquea `UPDATE`/`DELETE` mediante triggers tanto en PostgreSQL como
en SQLite.

Su downgrade toma lock exclusivo en PostgreSQL y falla cerrado si existe un
registro de journal o evidencia ARCA estructurada no nula. No puede usarse para
borrar historia ya aceptada. Si se requiere volver atrás después de que exista
esa evidencia, la alternativa segura es detener, conservar el estado y diseñar
una recuperación/restauración con datos y schema; no ejecutar un downgrade a
ciego.

### Ensayo obligatorio antes de un despliegue

1. Identificar el commit candidato y comparar el rango real desde `v0.2.2`.
2. Preparar un backup reciente, recuperable y privado; restaurarlo de forma
   aislada antes de tocar producción.
3. Ejecutar las migraciones en la restauración, desde la revisión real de
   origen hasta `c0d1e2f3a4b`, registrando `alembic current` y `heads`.
4. Verificar constraints, relaciones compuestas, ledger/cabezas, snapshots,
   guardas, `errores_arca_json`, journal y triggers append-only, sin exponer
   filas ni datos fiscales.
5. Ensayar el downgrade solo con datos sintéticos o una restauración aislada:
   confirmar el rollback permitido sin evidencia y confirmar el bloqueo cuando
   exista evidencia/journal.
6. Ejecutar smoke de stack y escenarios ARCA exclusivamente con dobles o
   consultas seguras autorizadas; no solicitar CAE.
7. Registrar hashes, rutas, dumps, credenciales y resultados detallados solo
   en la documentación privada del entorno.

## Matriz contable y QA PF-16G

Los escenarios usan datos sintéticos, fechas fiscales explícitas y dobles de
WSFE salvo que una fila indique una consulta segura autorizada. “Probado
previamente” identifica evidencia de una unidad ya cerrada en `main`, no una
revalidación del candidato completo. “No probado” es un requisito de cierre,
no una ausencia que pueda suplirse con una inferencia.

| Área | Escenario de aceptación para autoridad contable | Resultado esperado | Estado al 09/08/2026 |
|---|---|---|---|
| PF-02 individual | ARCA está un comprobante por delante del último local. | Se informa el próximo diagnóstico; no se reconstruye historia ni se persiste un número sin reserva. | Probado previamente; revalidación del rango pendiente. |
| PF-02 individual | ARCA cambia o falla entre el primer y segundo preflight. | Cero `FECAESolicitar`, cero comprobante nuevo; el número queda invalidado y se exige actualizar la numeración. | Probado previamente; revalidación del rango pendiente. |
| PF-02 batch/reintento | Un sublote o reintento encuentra historia externa o un estado local incierto. | Solo los grupos verdaderamente intactos pueden continuar el flujo seguro; `local_adelantada`, `en_proceso` o reconciliación bloquean sin nueva reserva. | Probado previamente; revalidación del rango pendiente. |
| PF-02 stale | Lote stale mixto con evidencia fuerte de una autorización y grupos intactos. | Se vincula la evidencia conocida; los intactos reencolan sin número, CAE, intento ni comprobante nuevos. | Probado previamente; revalidación del rango pendiente. |
| PF-03A | Request individual sintético contiene `monedaa`, `cotizaccion` o `confirmacion_fecha_fiscal` mal escrita. | `422 extra_forbidden`, sin defaults silenciosos, operación, reserva, intento ni CAE. | Probado previamente; revalidación del rango pendiente. |
| PF-03A | Snapshot batch stale o de reintento tiene una clave superior desconocida. | Falla cerrado, no consulta numeración ni emite; si una consulta previa confirma CAE, conserva esa evidencia para reconciliación. | Probado previamente; revalidación del rango pendiente. |
| PF-19A | Regla de contención sintética coincide exactamente con ambiente, emisor, punto y tipo. | Aborto antes de WSAA/WSFE, reserva, intento, comprobante y CAE; cambiar un componente deja la regla fuera de alcance. | Probado previamente; revalidación del rango pendiente. |
| PF-19A | Inventario legacy sobre SQLite sintética con firma exacta y variantes textuales. | Solo la firma global canónica es candidata; la ejecución es read-only, sanitizada y no modifica tablas ni consulta ARCA. | Probado previamente; revalidación del rango pendiente. |
| PF-19B | Constancia PDF sintética es completa, reciente y tiene señal exacta; el actor es admin en producción y confirma procedencia. | Solo entonces se obtiene `verificado_rece`; el PDF no persiste. Homologación y evidencia parcial quedan bloqueadas. | Probado previamente; revalidación del rango pendiente. |
| PF-19B | Cambia punto, ambiente, revisión, CUIT o autoridad después de la validación. | Individual, batch, worker, fallback, reintento y stale abortan antes de FECAE; un replay terminal durable no se reevalúa. | Probado previamente; revalidación PostgreSQL del rango pendiente. |
| PF-19B | Falla local al preparar/reservar un sublote ya después de lecturas seguras. | Cero guardas, intentos y reservas nuevos, y cero `FECAESolicitar`; WSAA o lecturas previas no se interpretan como emisión. | Probado previamente; revalidación del rango pendiente. |
| PF-19C | Doble WSFE devuelve cabecera `R` correlacionada y un solo `10005` entero, sin detalle ni CAE. | Solo el sublote enviado queda en rechazo global terminal; los posteriores quedan “no enviados”, con cero FECAE adicional. | Completo localmente; PostgreSQL real y CI pendientes. |
| PF-19C | Variantes `1005`, string, float, booleano, duplicado, mezcla, cabecera incoherente, detalle, CAE, timeout o Fault. | Ninguna es terminal: el grafo queda en reconciliación y el procesador se detiene. | Completo localmente; PostgreSQL real y CI pendientes. |
| PF-19C | Replay tras terminal o incertidumbre, carrera CAS y cambio de owner. | El replay devuelve estado durable sin WSAA/WSFE; un owner perdedor no publica ni emite sobre otro grafo. | Completo localmente; PostgreSQL real y CI pendientes. |
| PF-19C | Falla en cada frontera local posterior a la respuesta ARCA. | Intento, guarda, grupo/fila, lote, idempotencia y metadatos cierran juntos o revierten juntos; lo no confirmable queda bloqueado. | Completo localmente; PostgreSQL real y CI pendientes. |
| PF-19C legacy | `plan` sobre candidato sintético y `apply` con actor, confirmación y backup sintético verificable. | Plan read-only/determinista; solo ausencia comprobada en los ambientes requeridos permite `legacy_sin_autorizacion_verificada`; duda o autorización no mutan. | Completo localmente; PostgreSQL real y CI pendientes. |
| Migraciones | Upgrade/downgrade de ambas revisiones en SQLite sintética y PostgreSQL desechable. | Upgrade preserva invariantes; downgrade de PF-19C se bloquea ante journal/evidencia y nunca elimina historia aceptada. | SQLite local completo; ensayo PostgreSQL y restauración pendientes. |
| QA UI | Badges RECE, selectores y modal de importación con PDFs sintéticos. | La UI refleja `usable_factuflow` del servidor, no texto editable; no deja datos de otro emisor tras respuestas tardías. | Probado previamente para PF-19B; smoke candidato pendiente. |
| ARCA seguro | Consultas explícitas de lectura, si se autorizan para el ensayo o deploy. | Solo `FECompUltimoAutorizado` y `FECompConsultar` según el procedimiento; no se provoca `10005` ni se solicita CAE. | No probado para esta release; requiere autorización operativa separada. |

## Evidencia local congelada

Los siguientes resultados pertenecen a la validación local del corte
congelado. No acreditan CI, tag, publicación, despliegue ni el ensayo en una
restauración operativa.

| Control | Resultado local registrado | Alcance y límite |
|---|---|---|
| Backend | `1049 passed`, `22 skipped`, `31 warnings` en `9m14s`. | Los 22 omitidos corresponden a PostgreSQL sin URL/daemon local; no prueban la matriz PostgreSQL en CI. |
| Cobertura backend | Total branch-aware `69.2278%`; líneas `73.6741%`; ramas `55.1759%`; gate global `69%`. | Resultado del corte local; falta asociarlo al SHA candidato y a la ejecución CI. |
| Frontend | `30` archivos y `149` tests aprobados. | Validación local de la matriz frontend congelada. |
| Cobertura frontend | Statements `56.12%`, branches `50.14%`, functions `43.77%`, lines `57.37%`; gates `56/50/43/57`. | Resultado del corte local; falta asociarlo al SHA candidato y a la ejecución CI. |
| PF-16C | Node.js 24; auditorías npm de producción y desarrollo sin vulnerabilidades; unit, type-check, lint y build locales verdes. | Las ejecuciones son locales y no sustituyen los checks remotos. |
| PF-16E | Runtime Smoke unitario `8/8`, scripts `26/26` y reauditoría sin P0, P1 ni P2. | El smoke con PostgreSQL real sigue pendiente en CI. La reauditoría no se presenta como `autoreview`. |
| `autoreview` final | Primer intento con binario `0.130` detenido antes de revisar por catálogo `max`; reintento con binario instalado `0.147` y la misma configuración terminó con exit `0`. | TruffleHog limpio, bundle de `2.087.478` bytes, `5/5` pasadas, cero hallazgos y `overall patch is correct (0.98)`. Es evidencia local y no sustituye CI. |
| Seguridad final | Revisión separada posterior al fix de la CLI: P0/P1/P2 = `0`. | No sustituye la CI, la aceptación PF-16G ni el ensayo operativo. |

El comando efectivo del reintento fue
`python C:\Users\SANTI\.codex\skills\autoreview\scripts\autoreview --mode local --engine codex --model gpt-5.6-sol --thinking medium --codex-bin C:\Users\SANTI\AppData\Local\OpenAI\Codex\bin\cfac6bda2d141e07\codex.exe`.

## Validaciones técnicas y evidencia que debe recibirse antes de publicar

Aunque existe la evidencia local anterior, siguen pendientes resultados
reproducibles y atribuibles al commit candidato para:

- SHA del commit candidato integrado, SHA del tag base `v0.2.2`, rango real y
  `git status --short --branch` limpio en el punto de evaluación;
- inventario final de migraciones, dependencias, lockfiles, configuración,
  Docker/compose y archivos modificados desde la base;
- resultados atribuibles al candidato de PostgreSQL real, CI y ensayo de
  migración/restauración; la evidencia local PF-19C, `autoreview` final,
  backend, frontend, cobertura, scripts y Runtime Smoke unitario ya quedó
  registrada arriba;
- identificación de ejecución CI y resultado de cada check vigente:
  `Change Scope`, `Repository Checks`, `Backend Tests`, `Frontend Build`,
  `E2E Tests`, `Security Audit` y `Runtime Smoke`;
- asociación de las coberturas backend/frontend ya medidas al commit candidato
  y a su ejecución CI, más la cobertura específica de los núcleos fiscales
  modificados; no se reutilizan conteos o porcentajes de otro commit;
- evidencia de PostgreSQL 16 desechable para constraints, concurrencia,
  migraciones y la matriz PF-19C; los 22 skips locales por falta de URL/daemon
  no acreditan esa ejecución;
- resultado del check `Runtime Smoke` con PostgreSQL real y evidencia de su
  comportamiento integrado; este smoke sigue pendiente en CI;
- revisión documental del rango, privacidad, idioma y ausencia de secretos,
  datos fiscales reales, PDFs, backups, logs o evidencia privada;
- resultados sanitizados del ensayo de backup, restauración aislada,
  preflight, upgrade, downgrade/rollback y smoke operativo.

No se debe convertir un intento fallido de revisión, un test omitido, una
ejecución local parcial, un build, un health local o una prueba PostgreSQL
efímera en afirmación de CI, cobertura, release o despliegue.

## Riesgos residuales y tratamiento

| Riesgo residual | Tratamiento antes de avanzar |
|---|---|
| PF-19C requiere evidencia externa. | Ejecutar PostgreSQL desechable y CI sobre el commit candidato, además del ensayo de migración/restauración; mantener el corte sin declarar listo hasta clasificarlos. |
| Migraciones fiscales con datos legacy reales. | Backup recuperable, restauración aislada, preflight y validación de schema/datos antes de producción; no corregir por inferencia. |
| Downgrade después de evidencia PF-19C. | Tratarlo como bloqueado por diseño; preferir rollback de aplicación compatible o restauración planificada, nunca borrar journal/evidencia. |
| Diferencia entre `main`, release y VPS. | Identificar commits/tags reales y registrar por separado código integrado, tag publicado y target efectivamente desplegado. |
| Elegibilidad RECE o error ARCA observado en un entorno real. | No provocar CAE ni `10005`; usar dobles para QA y solo consultas seguras explícitamente autorizadas para diagnóstico. |
| Cobertura y smoke integral PF-16 pendientes de atribuir al candidato. | La cobertura local ya está medida; asociarla al SHA y CI candidatos y completar Runtime Smoke con PostgreSQL real antes de declarar cierre. |
| Backups con custodia y rutas privadas. | Conservar evidencia detallada fuera del repositorio; el dossier público solo registra resultados sanitizados. |

## Puertas de decisión y autorizaciones separadas

Antes de declarar este candidato listo deben estar cerrados el alcance, las
pruebas Nivel 2, CI, seguridad, cobertura aplicable, PostgreSQL, documentación,
backup/restauración, migraciones y la matriz anterior. La evidencia local
registrada no elimina estas puertas. No debe haber P0 ni P1 bloqueante conocido
dentro del rango.

Las siguientes decisiones no son equivalentes y requieren autorización explícita
por separado:

1. **Tag:** crear el tag inmutable `v0.3.0` sobre un commit exacto ya integrado.
2. **Publicación:** crear la GitHub Release/notas asociadas al tag; esto no
   actualiza ningún VPS.
3. **Despliegue:** actualizar una instalación concreta desde el tag/commit
   identificado, con backup final, preflight inmediato, migraciones una sola
   vez y QA post-deploy sanitizada.
4. **Cualquier prueba con CAE real:** decisión fiscal adicional, específica y
   previa; no está incluida en las tres autorizaciones anteriores.

## Estado de cierre de este dossier

El contenido estable, las invariantes, el plan de migración/rollback, la matriz
de aceptación y la evidencia local indicada quedaron congelados para revisión.
Permanecen pendientes los datos enumerados en “Validaciones técnicas y
evidencia que debe recibirse antes de publicar”; hasta recibirlos, no
corresponde marcar ninguna puerta de candidato listo, tag, publicación o
despliegue como aprobada.
